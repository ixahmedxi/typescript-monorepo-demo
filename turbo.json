{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "//#root:codegen": {
      "cache": true,
      "persistent": false,
      "inputs": ["$TURBO_DEFAULT$", "./scripts/**/*"],
      "outputs": ["generated-routers/**"]
    },
    "build": {
      "dependsOn": ["^build", "//#root:codegen"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [
        ".next/**",
        "!.next/cache/**",
        "dist/**",
        "tsconfig.tsbuildinfo"
      ],
      "persistent": false,
      "cache": true
    }
  }
}
