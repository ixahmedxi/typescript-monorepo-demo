{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "//#codegen": {
      "cache": true,
      "inputs": ["$TURBO_DEFAULT$", "./scripts/**/*"],
      "outputs": ["generated-routers/**"]
    },
    "build": {
      "dependsOn": ["^build", "//#codegen"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [
        ".next/**",
        "!.next/cache/**",
        "dist/**",
        "tsconfig.tsbuildinfo"
      ],
      "persistent": false,
      "cache": true
    }
  }
}
